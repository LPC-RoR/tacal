<% if delete == true %>
	<%= link_to link, method: :delete, data: { confirm: 'Está seguro?'}, class: "btn btn-outline-#{clr} btn-sm" do %>
	    <i class="glyphicon glyphicon-euro"></i> <%= texto %>
	<% end %>
<% else %>
	<%= link_to link, data: { turbolinks: false }, class: "btn btn-outline-#{clr} btn-sm " do %>
	    <i class="glyphicon glyphicon-euro"></i> <%= texto %>
	<% end %>
<% end %>
