<p id="notice"><%= notice %></p>

<h1>B Palabras</h1>

<table>
  <thead>
    <tr>
      <th>B palabra</th>
      <th>B clave</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @b_palabras.each do |b_palabra| %>
      <tr>
        <td><%= b_palabra.b_palabra %></td>
        <td><%= b_palabra.b_clave_id %></td>
        <td><%= link_to 'Show', b_palabra %></td>
        <td><%= link_to 'Edit', edit_b_palabra_path(b_palabra) %></td>
        <td><%= link_to 'Destroy', b_palabra, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New B Palabra', new_b_palabra_path %>
