<%= form_with(model: b_palabra, local: true) do |form| %>
  <% if b_palabra.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(b_palabra.errors.count, "error") %> prohibited this b_palabra from being saved:</h2>

      <ul>
      <% b_palabra.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :b_palabra %>
    <%= form.text_field :b_palabra %>
  </div>

  <div class="field">
    <%= form.label :b_clave_id %>
    <%= form.number_field :b_clave_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
