<%= form_with(model: st_perfil_estado, local: true) do |form| %>
  <% if st_perfil_estado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(st_perfil_estado.errors.count, "error") %> prohibited this st_perfil_estado from being saved:</h2>

      <ul>
      <% st_perfil_estado.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :st_perfil_estado %>
    <%= form.text_field :st_perfil_estado %>
  </div>

  <div class="field">
    <%= form.label :rol %>
    <%= form.text_field :rol %>
  </div>

  <div class="field">
    <%= form.label :st_perfil_modelo_id %>
    <%= form.number_field :st_perfil_modelo_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
